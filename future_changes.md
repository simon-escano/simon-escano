changes:
general:
- [x] make it so that the max content width is 1280px, otherwise sides will have margin in them auto.
- [x] when scrolling down on the page, instead of the header element's background get solid, make it so that scrolling down will make the header into a floating header (glassy) in the max content width, make sure the transition has smooth motion

homepage:
- [x] make it so that the Card Swap elements for the top projects is not contained within a div/box, to reduce redundancy for the layout
- [x] make it so that the tech stack, for each category, it should not have be contained within a box to reduce redundancy in the layout.

projects page:
- [x] make it so that the preview photos for every project, if it has multiple photos, make it a collage (max of 3, +n if more than 3) instead of just displaying one photo for each project card.

about page:
- [x] for the numerics, it should not be contained in cards, just make it have no background,padding,and stroke to save space.

contact page:
- [x] add a copy button for the mobile number as well
- [x] instead of "Active Opportunity Window / Currently reviewing...", just say: "Open to work", no subtext
