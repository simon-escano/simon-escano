Hide API keys behind environment variables and serverless functions (process.env / Cloudflare Secrets).

Purge Git secrets using tools like git-filter-repo and maintain a strict .gitignore.

Enforce HTTPS & HSTS (Strict-Transport-Security: max-age=31536000; includeSubDomains; preload).

Add strict security headers (Content-Security-Policy, X-Frame-Options: DENY, X-Content-Type-Options: nosniff).

Lock down CORS policies on serverless functions to only allow requests from your portfolio domain.

Add bot protection & rate limits (e.g., Cloudflare Turnstile or honeypots) on contact endpoints.

Validate, sanitize, and limit request payloads for all form submissions to prevent memory exhaustion and injection attacks.

Trim API responses server-side so only required fields are sent over the wire.

Scan dependencies regularly using npm audit or automated Dependabot checks.